{
  "name": "labo-smart-home",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --write . && uv run ruff format lsh-stack.py src tests",
    "format:check": "prettier --check .",
    "lint:md": "markdownlint-cli2",
    "lint:md:fix": "markdownlint-cli2 --fix",
    "format:py:check": "uv run ruff format --check lsh-stack.py src tests",
    "lint:py": "uv run ruff check lsh-stack.py src tests",
    "lint:py:fix": "uv run ruff check --fix lsh-stack.py src tests",
    "typecheck": "uv run mypy src/lsh_stack_config",
    "test:py": "uv run pytest",
    "smoke:firmware": "./scripts/build_and_smoke_zipapp.sh",
    "check:py": "npm run format:py:check && npm run lint:py && npm run typecheck && npm run test:py",
    "check": "npm run format:check && npm run lint:md && npm run check:py",
    "fix": "npm run format && npm run lint:md:fix && npm run lint:py:fix"
  },
  "overrides": {
    "markdownlint-cli2": {
      "smol-toml": "^1.8.0"
    }
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.23.2",
    "prettier": "^3.9.8"
  }
}
